# Required test credentials
BLS_EMAIL=your_test_email@example.com
BLS_PASSWORD=your_test_password

# Optional: force dynamic field names if the site markup changes and heuristic selection fails
# BLS_STAGE1_EMAIL_FIELD=
# BLS_STAGE2_PASSWORD_FIELD=

# Proxy configuration. Keep one proxy for one full session.
# Format: http://username:password@host:port
BLS_PROXY_URL=http://jkaLNM653:nlhNBP748@212.174.10.112:40511

# Optional runtime settings
BLS_TIMEOUT=30
BLS_DEBUG_HTML=false
